  type Query {
    users: [User]
    companies: [Company]
    user(id: ID!): User
    company(id: ID!): Company
  }

  type Mutation {
    createUser(user: UserInput): User
    updateUser(user: UserInput): SuccessResponse
  }

type Response1 {
  success: Boolean!
}

  type User{
    id: ID!
    firstName: String
    lastName: String
    email: String
    age: Int
    companyCode:String
    company: Company
  }
type Response2 {
  success: Boolean!
  message: String
  error: String
}

union SuccessResponse = Response1 | Response2

  type Company{
    code: ID!
    name: String
  }
  
  input UserInput {
    userId: ID!
    firstName: String
    lastName: String
    email: String
    age: Int
    companyCode:String
  }